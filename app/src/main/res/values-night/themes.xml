<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FileManager" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:textColor">@color/black</item>

        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/gray_800</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:background">@color/black</item>
    </style>

    <style name="CardviewStyle" parent="Widget.MaterialComponents.CardView">
        <item name="elevation">
            8dp
        </item>
        <item name="cardUseCompatPadding">
            true
        </item>
        <item name="cardCornerRadius">
            4dp
        </item>
    </style>

    <item name="bar_text_color" type="color">@color/white</item>
    <item name="img_button_bg" type="color">@color/black</item>
    <color name="cardView_color">@color/pale_black</color>


</resources>